#include <iostream>
#include <string>

using namespace std;

int main()
{
	setlocale(0, "rus");
	const int n = 10;
	std::string arr[n];
	for (int i = 0; i < n; i++)
	{
		arr[i] = (i % 2 ==0) ? "Yes" : "No";
		std::cout << arr[i] << std::endl;
	}
	system("pause");
	return 0;
}
